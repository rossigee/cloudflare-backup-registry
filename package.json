{
  "name": "cloudflare-backup-registry",
  "version": "0.5.0",
  "description": "Cloudflare Workers-based backup run registry",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:golder": "wrangler deploy --env golder",
    "deploy:wardle": "wrangler deploy --env wardle",
    "deploy:timewarp": "wrangler deploy --env timewarp",
    "build": "wrangler deploy --dry-run --env golder",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "jose": "^6.2.3"
  }
}
